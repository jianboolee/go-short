# 自定义域名（必填，用于生成短链接URL）
# 例如: short.example.com 或 localhost:8080
DOMAIN=

# 基础路径（可选，默认为空）
# 例如: /s/ 表示短链接在 /s/xxx
# 留空表示短链接在根路径 /xxx
BASE_PATH=

# 服务端口（可选，默认 8080）
PORT=8080

# 数据库路径（可选，默认 ./data/shortlinks.db）
DB_PATH=./data/shortlinks.db

# 短链接码长度（可选，默认 4）
CODE_LENGTH=4

# API Key（可选，如果设置则需要验证）
# 创建短链接时需要在请求头中添加 X-API-Key 或 Authorization
# 例如：X-API-Key: your-secret-key
# 或：Authorization: Bearer your-secret-key
API_KEY=
